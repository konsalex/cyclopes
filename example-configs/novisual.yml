# Directory where the images will be saved
# or retrieved from the channel adapters
imagesDir: "./images"
# Adapters
# Each adapter will have it's own parameters
adapters:
  # Slack adapter will try to get ENV variables
  # for oauth_token -> ADAPTERS_SLACK_OAUTH_TOKEN
  # for channel_id -> ADAPTERS_SLACK_CHANNEL_ID
  slack: []
